<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Green Labs | {{title}}</title>
    <link rel="stylesheet" href="/css/styles.css">
    <script src="/js/bootstrap.bundle.min.js"></script>
</head>

<body>
    <div class="container-fluid vh-100 w-100 d-flex flex-column align-items-center justify-content-center bg-dark p-0 m-0"
        style="background: linear-gradient(rgba(8, 120, 48, 0.1), rgba(0, 0, 0, 0.3)), url('./imgs/bg.jpg') center/cover no-repeat;">

        <div class="row w-100 justify-content-center">
            <div class="d-flex align-items-center justify-content-center py-3 mb-3">
                <img src="imgs/logo.png" alt="Green Labs Logo"
                    class="rounded-circle bg-white border border-white border-2 p-2" width="100" height="100">
                <h1 class="text-white fs-1 fw-bold ms-3">Green Labs</h1>
            </div>

            <div class="bg-black bg-opacity-75 rounded-4 shadow-lg p-5" style="max-width: 450px; width: 90%;">
                {{#if errorMessage}}
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                    <strong>Error:</strong> {{errorMessage}}
                </div>
                {{/if}}
                
                {{{ body }}}
            </div>
        </div>
    </div>

</body>

</html>